name: connector-utils
lang: unknown
lang_version: unknown
git:
  enable: true
codeowners:
  enable: true
renovatebot:
  enable: true
  automerge:
    enable: true
code_artifact:
  enable: true
  package_paths:
    - "maven-snapshots/maven/io.confluent/connect-utils*"
